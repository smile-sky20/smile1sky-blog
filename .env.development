# Next.js 可以同时支持构建时和运行时环境变量。

# 默认情况下，环境变量只在服务器端可用。
# 要将环境变量暴露给浏览器，必须以 NEXT_PUBLIC_ 为前缀。
# 但是，这些公共环境变量将在 next build 期间被内联到 JavaScript 包中。
SERVER_BASE_PATH="http://localhost:3220"
NEXT_PUBLIC_API_BASE_URL = 'http://localhost:3220'